// Re-export useUser hook from UserContext for convenience
// This follows the project structure convention of having hooks in the hooks/ directory
export { useUser, UserProvider } from '@/lib/contexts/UserContext'
